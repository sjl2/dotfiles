[user]
  name = Stewart Lynch
  email = stewart.j.lynch@gmail.com
[credential]
  helper = osxkeychain
[push]
  default = current
[branch]
  autosetupmerge = true
[alias]
	fixup = commit --amend -c HEAD
  prune = remote prune origin                         ; Prune local branches to match origin
  recommit = commit -c ORIG_HEAD                      ; Commit current changes as recently removed head
  show-files = show --name-only                       ; Show names of files updated in last commit
  uncommit = reset --soft HEAD^                       ; Move last commit's changes to the staging area
  unstage = reset HEAD .                              ; Unstage all files
  wip = commit -m "wip"                               ; Save currently staged work as WIP
  pushf = push --force-with-lease --force-if-includes ; Only force pushes if safe with origin
[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
  pager = true
[pager]
  branch = false ;
[color "diff"]
  meta = magenta
[core]
[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
[pull]
	rebase = true
	ff = only
[coderabbit]
	machineId = cli/16625299bb1d4a3ba8b3f40f42023264
